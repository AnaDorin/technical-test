import React from 'react';
import ReactDOM from 'react-dom';
// here will be the sass file
import App from './pages/App';

ReactDOM.render(
  <React.StrictMode>
    <App />
  </React.StrictMode>,
  document.getElementById('root')
);
